# PawTrace 환경변수 예시 — 복사해서 .env 로 사용하세요. (.env 는 커밋 금지)
APP_NAME=PawTrace API
API_V1_PREFIX=/api/v1

# DB / 캐시
DATABASE_URL=postgresql+psycopg2://pawtrace:pawtrace@db:5432/pawtrace
REDIS_URL=redis://cache:6379/0

# 외부 연동 (없으면 stub 모드)
PUBLIC_DATA_API_KEY=
AWS_S3_BUCKET=
BEDROCK_MODEL_ID=
